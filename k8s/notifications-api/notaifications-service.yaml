apiVersion: v1
kind: Service
metadata:
  name: notifications-api-service
  namespace: microservices
  labels:
    app: notifications-api
spec:
  selector:
    app: notifications-api
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  type: ClusterIP
  sessionAffinity: None